# Features

* generators
* secrets handling, including symmetric keys, server can generate keys that only nodes can decrypt
* works on either a push or pull model
* simple declarative DSL, extendable via libraries
* idempotence configurable, can be set to active or passive, where active will continuously configure, passive will configure then notify on discrepancies
