# Different tools can be built based on the roles

## CICD

trigger: none

hosts get designated a CICD role, this can help configure the host with build software
server can push code to the host

## config management

trigger: cron
hosts get jobs and roles, trigger

## deploy
